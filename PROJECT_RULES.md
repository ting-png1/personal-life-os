# Personal Life OS — 项目开发规则

> **定位**：AI 在修改本项目时必须遵守的规则。描述"修改项目时应该怎么做"。
> **与 PROJECT_PLAN 的关系**：PROJECT_PLAN 描述"项目现在是什么样"，本文档描述"修改项目时应该怎么做"。
> **最后更新**：2026-08-30（文档体系整理）

---

## 总原则

**实际代码状态、运行结果和测试结果优先于项目文档。**

如果 PROJECT_PLAN 与实际项目状态发生冲突，必须以实际代码为准，并在完成任务后修正文档。不得为了"符合文档"而修改正确的代码。

---

## 零、开发流程（本地验证优先 — 铁律）

**禁止每次修改代码后直接 push 触发 Netlify 生产部署。**

### 标准开发流程

```
修改代码
    ↓
本地 TypeScript 检查 (npx tsc --noEmit)
    ↓
本地生产构建 (npm run build)
    ↓
本地预览验证 (npm run preview)
    ↓
确认功能正常、无报错
    ↓
Git commit（可以正常提交）
    ↓
【等待用户确认"可以发布了"】
    ↓
Git push → Netlify 生产部署
```

### 硬性要求

1. **每次修改后必须本地验证**：至少执行 `npx tsc --noEmit` + `npm run build`，确认无错误
2. **禁止自动 push**：开发过程中可以正常 `git commit`，但**不要自动 `git push`** 到生产分支
3. **生产部署需用户确认**：只有用户明确说"可以发布了"之后，才执行 `git push` 触发生产部署
4. **PWA/Service Worker 调试期间尤其严格**：在确认 React 挂载、AppInitializer、App READY、本地 build、Safari 打开、PWA standalone、Service Worker 全部正常之前，不要触发生产部署

### 本地验证工具

| 命令 | 用途 |
|---|---|
| `npx tsc --noEmit` | TypeScript 类型检查 |
| `npm run build` | 生产构建 |
| `npm run preview` | 本地预览生产构建（默认 http://localhost:4173） |
| `netlify dev` | 本地模拟 Netlify 环境（如需） |
| `netlify deploy` | 创建 Draft Deploy 预览（不影响生产） |
| `netlify deploy --prod` | 发布到生产站（需用户确认） |

### Draft Deploy（可选）

如果需要手机实际测试，但不想影响生产站，可以使用 Netlify CLI 创建 Draft Deploy：
```bash
netlify deploy  # 创建临时预览链接，不影响生产
```
Draft Deploy 会生成一个临时预览 URL，可以用于手机测试，确认无误后再 `netlify deploy --prod` 发布。

### 违反规则的后果

- 频繁生产部署会导致：Service Worker 缓存混乱、用户端版本不一致、调试困难
- PWA 调试期间，每次生产部署都可能让已安装的 PWA 进入不确定状态
- 因此，**本地验证优先，生产部署谨慎**

---

## 一、权限边界

### 1.1 可自由修改的区域

| 区域 | 说明 |
|---|---|
| `src/features/*/components/` | 各模块的 UI 组件 |
| `src/shared/ui/` | 共享 UI 组件（扩展时需注意不破坏已有 API） |
| `src/styles/` | 样式文件和 Design Token |
| `src/features/*/hooks/` | 各模块的 Hook |
| 文档文件 | PROJECT_PLAN.md / PROJECT_RULES.md / CHANGELOG.md |
| 配置文件 | vite.config.ts / tailwind.config.js / tsconfig.json（非架构性调整） |

### 1.2 需谨慎修改的区域（修改前必须分析影响）

| 区域 | 原因 |
|---|---|
| `src/features/*/types.ts` | 数据模型变更影响 Repository、Store、UI、未来同步 |
| `src/features/*/repository.ts` | 数据访问层变更影响所有上层 |
| `src/features/*/store.ts` | 状态管理变更影响所有订阅组件 |
| `src/data/database.ts` | 数据库表结构变更需要版本迁移 |
| `src/features/today/aggregator/TodayAggregator.ts` | 核心聚合逻辑，影响 Today 页面所有数据 |
| `src/features/*/services/*.ts` | 纯函数业务逻辑，变更可能影响多个调用方 |

### 1.3 禁止擅自修改的区域（必须先说明并获得确认）

| 区域 | 原因 |
|---|---|
| 五层架构的分层规则 | 架构根基，变更影响整个项目 |
| 核心数据模型的字段删除/类型变更 | 可能导致已有数据丢失或不兼容 |
| 新增第三方依赖 | 影响包体积、构建、未来维护 |
| 数据库版本迁移逻辑 | 错误的迁移会导致用户数据损坏 |
| 用户隐私相关逻辑（情绪/周期/健康数据处理） | 敏感数据，变更需用户知情 |
| AI 相关的 API Key 处理逻辑 | 安全相关，变更可能导致 Key 泄露 |

### 1.4 高风险操作

以下操作属于高风险，执行前必须明确说明操作内容、影响范围和回滚方案：
- 删除数据库表或字段
- 大规模重构（跨 3 个以上模块）
- 修改构建配置导致产物结构变化
- 修改 PWA Service Worker 逻辑
- 任何可能导致用户已有数据丢失的操作

---

## 二、修改前分析

收到任务后**不要立即修改代码**。按以下步骤分析：

### 2.1 定位相关文件

1. 先读取 PROJECT_PLAN.md 了解当前架构和模块划分
2. 用关键词搜索定位相关文件（Grep / Glob）
3. 读取相关文件的完整内容，不凭印象修改

### 2.2 理解依赖关系

修改前必须回答：
- 这个文件被哪些其他文件 import？
- 这个模块的数据流是怎样的？（UI → Hook/Store → Domain → Repository → Infrastructure）
- 修改这个函数/组件/类型会影响哪些调用方？
- 是否有跨模块联动？（如修改 Todo → Today 聚合 → AI 输入）

### 2.3 判断影响范围

- **局部修改**：只影响当前组件/函数内部，可直接修改
- **模块内修改**：影响同一模块的其他文件，需检查模块内所有引用
- **跨模块修改**：影响其他模块，需检查所有跨模块引用并验证联动
- **架构修改**：影响分层规则或核心设计，必须暂停并向用户请示

---

## 三、修改后自检

完成修改后必须进行适当的检查，**不得默认任务已经成功**。

### 3.1 必做检查

| 检查项 | 命令/方式 | 说明 |
|---|---|---|
| TypeScript 类型检查 | `npx tsc --noEmit` | 必须无错误 |
| 生产构建 | `npm run build` | 必须成功，检查构建产物体积 |
| 相关功能验证 | 浏览器手动测试 | 修改的功能必须正常工作 |

### 3.2 根据修改范围增加检查

- **修改了共享组件**：检查所有使用该组件的页面是否正常
- **修改了数据模型**：检查所有使用该类型的文件，验证数据库读写正常
- **修改了 Store/Repository**：检查相关模块的 CRUD 操作是否正常
- **修改了 TodayAggregator**：检查 Today 页面所有数据（情绪/日程/待办/进度）是否正确
- **修改了样式/Design Token**：检查所有页面视觉是否正常，无颜色错乱
- **修改了 PWA 配置**：检查 manifest、Service Worker、图标是否正常

### 3.3 发现问题的处理

- 发现问题必须继续修复，不得带着已知问题结束任务
- 无法在当前任务范围内修复的问题，必须明确报告：问题描述、影响范围、建议的修复方案
- 修复问题后必须重新运行所有相关检查

### 3.4 文档同步

完成重要任务后必须更新 PROJECT_PLAN.md：
- 新增模块/功能 → 更新模块划分和任务清单
- 修改数据模型 → 更新数据模型章节
- 架构决策变更 → 更新 ADR 和修订日志
- 发现已知问题 → 更新风险登记或已知问题列表

**不得虚构已经完成的功能**。文档必须如实反映实际代码状态。

---

## 四、防止功能回归

修改一个功能时必须注意**不要破坏已有功能**。

### 4.1 高风险修改场景

| 场景 | 风险 | 额外检查 |
|---|---|---|
| 修改共享组件（GlassCard/GlassButton 等） | 所有使用该组件的页面可能受影响 | 遍历所有页面验证组件正常 |
| 修改 Design Token（颜色/间距/圆角） | 全局视觉可能错乱 | 检查所有主要页面视觉 |
| 修改 Store 的 action 签名 | 所有调用方可能报错 | 搜索所有调用点并验证 |
| 修改 Repository 接口 | 所有上层依赖可能断裂 | 检查所有实现和调用 |
| 修改数据库表结构 | 已有数据可能不兼容 | 必须写版本迁移，验证升级流程 |
| 修改 TodayAggregator | Today 页面所有数据可能错误 | 验证情绪/日程/待办/进度全部正确 |
| 修改路由配置 | 页面可能无法访问 | 验证所有路由可正常访问 |

### 4.2 回归检查清单

每次涉及公共组件、核心模块、数据库、跨模块逻辑的修改后，必须检查：
- [ ] Today 页面正常显示（情绪/日程/待办/进度）
- [ ] Schedule 页面正常（周视图/添加/编辑/删除）
- [ ] Todo 页面正常（列表/筛选/添加/完成/删除）
- [ ] Wellness 页面正常（情绪记录/周期记录）
- [ ] Settings 页面正常（数据导出/AI 配置）
- [ ] 底部 TabBar 导航正常
- [ ] PWA 构建正常（manifest + SW + 图标）

---

## 五、任务边界

**只完成当前任务所需要的修改**，不得擅自扩大范围。

### 5.1 禁止的行为

- ❌ 为了"顺便优化"而重构无关代码
- ❌ 修改未被要求改动的模块
- ❌ 改变既有的产品设计（如擅自修改页面布局、交互方式）
- ❌ 新增未被要求的功能
- ❌ 升级未被要求的依赖版本
- ❌ 修改文件结构（移动/重命名文件）除非任务明确要求

### 5.2 允许的行为

- ✅ 为了完成当前任务必须进行的关联修改（如新增组件需要新增类型定义）
- ✅ 修复在当前任务过程中发现的、与当前任务直接相关的 Bug
- ✅ 对当前任务涉及的代码进行必要的小范围重构以提高可读性

### 5.3 发现可优化项的处理

在当前任务过程中发现与任务无关的可优化项：
1. 记录下来（在任务报告中说明）
2. 不立即修改
3. 建议用户在后续任务中处理

---

## 六、Agent 循环与成本保护

### 6.1 禁止无限重试

- 同一操作连续失败 **2 次**，必须停止并分析原因，不得无脑重试
- 同一类型的错误连续出现，必须查找共同成因而非逐个绕过
- 工具调用超时后，先检查操作是否实际已执行，再决定是否重试

### 6.2 禁止无意义的反复修改

- 不得在没有明确改进目标的情况下反复修改同一文件
- 不得为了"看起来在工作"而进行无意义的编辑
- 样式微调最多 2-3 轮，超过后应暂停并向用户确认方向

### 6.3 连续失败时的处理流程

1. 停止当前操作
2. 分析失败原因（读取错误信息、检查相关代码）
3. 如果是环境/权限问题 → 明确报告并等待用户指示
4. 如果是代码逻辑问题 → 修复后重试
5. 如果连续 3 次无法解决 → 暂停并向用户报告：问题描述、已尝试的方案、建议的下一步

### 6.4 任务报告要求

每个任务完成后必须报告：
- 修改了哪些文件
- 完成了什么内容
- 进行了哪些自检（tsc/build/手动测试）
- 是否发现问题
- 项目文档是否已同步更新

---

## 七、编码规范

### 7.1 TypeScript

- strict 模式，禁止 `any`（必要时用 `unknown` + 类型守卫）
- 所有函数参数和返回值有明确类型
- 组件文件不超过 300 行，超过则拆分
- `page.tsx` 类文件极薄，只做布局组合，不写业务逻辑

### 7.2 导入与命名

- 导入顺序：外部库 → `@/shared` → `@/features` → 相对路径
- 文件命名：组件用 PascalCase（`TodoItem.tsx`），工具/类型/服务用 camelCase（`date.ts`、`types.ts`）
- 变量名/函数名用英文，语义清晰

### 7.3 Git

- 每个任务完成后 commit，message 格式：`feat: 模块 - 任务描述` 或 `fix: 模块 - 修复内容`
- 不在 commit 中包含 `node_modules/`、`dist/`
- `.gitignore` 包含：node_modules, dist, .env, *.local

---

## 八、用户可见文字统一中文（硬性要求）

**所有用户能够看到的文字必须使用中文。** 这是硬性要求，无例外。

### 必须中文

- 底部导航标签：今日 / 日程 / 待办 / 状态 / 更多
- 按钮文字：添加 / 保存 / 取消 / 删除 / 编辑
- 页面标题、区块标题
- 空状态文案
- 表单标签、占位符
- 错误提示、加载提示
- 情绪等级：很糟 / 不好 / 平稳 / 不错 / 很好
- 日程类型：课程 / 个人 / 休息 / 其他
- 优先级：高 / 中 / 低
- 设置页、关于页全部文字
- 确认弹窗的标题和内容

### 继续使用英文（代码内部）

- 文件名、TypeScript 类型/接口名、变量名、函数名、Component 名称
- CSS 类名、Tailwind 类名
- 路由路径
- 控制台日志（开发调试用）
- 代码注释（可中文可英文，建议中文）

---

## 九、架构约束（铁律）

1. UI 层不直接 import Dexie / Supabase
2. Domain 层纯函数不 import React / DOM / Dexie
3. Repository 上层只依赖接口，不依赖具体实现
4. 模块间不直接 import 对方的内部组件/Store
5. TodayState 不入库、不持久化、不是数据库表
6. AI 不直接修改业务数据，只产生建议
7. 所有颜色通过 Design System token 引用，禁止硬编码
8. 所有页面使用 `shared/ui/` 组件，禁止自写 `<button>`/`<input>`
9. 依赖方向永远向下：UI → Hook/Store → Domain → Repository → Infrastructure
10. 禁止反向依赖和跨层跳跃

---

## 十、文档使用流程

每次开始任务前：
1. 读取 PROJECT_RULES.md（本文档）了解开发规则
2. 读取 PROJECT_PLAN.md 了解当前项目状态

每次完成任务后：
1. 自检（tsc + build + 手动测试）
2. 更新 PROJECT_PLAN.md（如有状态变更）
3. 追加 CHANGELOG.md（如有重要变更）
4. 报告任务结果

需要了解历史变更时才读取 CHANGELOG.md。

---

## 十一、经验教训与问题解决公式

### 11.1 外部服务集成检查清单

接入任何外部服务（Supabase / AI API / 通知 / 第三方 SDK）时，必须逐项检查：

1. **权限模型**：该服务要求哪些权限字段？（如 Supabase RLS 要求 `user_id`）
2. **字段映射**：本地字段名与服务端字段名是否一致？（camelCase ↔ snake_case）
3. **错误处理**：服务调用失败时是否有明确的错误信息？是否会静默失败？
4. **双向验证**：写入和读取是否都测试过？（只测拉取不测推送会漏掉一半问题）
5. **全量兜底**：增量同步可能丢失数据时，是否提供全量同步功能？
6. **多端验证**：是否在目标平台（手机/电脑）都验证过可用性？
7. **初始化安全降级**：环境变量未配置 / 服务不可用时，是否返回 null 或 mock 对象？**绝对不能在模块顶层调用可能抛异常的函数**
8. **模块加载异常防护**：外部服务客户端初始化必须放在条件判断中，未配置时不调用初始化函数，避免模块加载失败导致整个应用崩溃
9. **console.warn 不是错误处理**：检查到问题后只警告但继续执行危险操作，等于没有检查
10. **本地正常 ≠ 生产正常**：本地 `.env` 配置了环境变量，但生产环境（Netlify/Vercel）可能没配置，必须在两种环境下都测试

### 11.2 PWA 开发检查清单

开发或修改 PWA 相关功能时，必须逐项检查：

1. **React 挂载优先**：`ReactDOM.createRoot().render()` 必须在所有第三方初始化（registerSW、统计、广告等）之前执行。任何可能抛异常的同步调用，都不能阻塞 React 挂载。
2. **内联 App Shell**：`index.html` 中必须有内联的加载界面，不能依赖 JS 渲染第一个像素
3. **Service Worker 注册**：必须手动注册 `registerSW`，使用动态 import + try-catch，不能同步调用
4. **旧缓存清理**：必须配置 `cleanupOutdatedCaches: true`，否则旧缓存会导致新 JS 404
5. **离线回退**：必须配置 `navigateFallback: '/'`，PWA 启动时路由不能失败
6. **更新机制**：`registerType: 'autoUpdate'` + `skipWaiting()` + `clientsClaim()`，确保新版本能立即激活
7. **初始化超时**：任何异步初始化（本地数据/云端同步）都必须有超时保护，不能永久 loading
8. **云端同步非阻塞**：云端同步必须在后台执行，绝对不能阻塞首屏渲染
9. **失败静默降级**：云端同步失败时必须静默降级，不能显示错误页面影响使用
10. **真实设备验证**：PWA 问题必须在真实手机上验证，浏览器模拟器不能完全复现主屏幕 App 行为
11. **部署后验证**：每次部署后必须验证：新版本是否能被已安装的 PWA 自动获取
12. **启动诊断日志**：在启动关键节点加 console.log，远程排查问题时唯一的线索就是日志
13. **双重超时保护**：Promise.race 超时 + 安全兜底定时器，确保即使 Promise.race 有问题，也能强制进入 App
14. **桌面正常 ≠ 移动端正常**：Service Worker、IndexedDB、localStorage 在 standalone 模式下可能有差异

### 11.2 问题解决公式（排查步骤）

遇到"功能不生效"类问题时，按以下顺序排查：

```
1. 确认现象 → 具体是什么不生效？有报错吗？
2. 检查数据流 → 数据从哪里来？到哪里去？哪一步断了？
3. 检查权限 → 是否有权限访问/写入？RLS/API Key 是否正确？
4. 检查字段 → 字段名/类型/必填项是否匹配？
5. 检查网络 → 请求是否发出？响应是什么？
6. 检查日志 → 控制台/网络面板是否有错误？
7. 最小复现 → 用最简单的操作复现问题
8. 修复 → 针对根本原因修复，不是表面症状
9. 验证 → 修复后在真实环境验证，不只是类型检查通过
```

### 11.3 同步/数据相关经验

1. **静默失败最危险**：推送失败但没有明显报错，用户以为同步成功了，实际数据没上去
2. **RLS 是隐形门槛**：Supabase RLS 策略不满足时，请求返回空或报错，必须仔细检查策略
3. **user_id 不能忘**：所有业务表都有 `user_id`，写入时必须设置，否则 RLS 拒绝
4. **全量同步是安全网**：当增量同步可能丢失数据时，提供"全量推送+全量拉取"按钮
5. **先推送后拉取**：手动同步时，先把本地数据推上去，再拉云端数据下来，避免本地新数据被旧数据覆盖
6. **部署后立即多端验证**：Netlify/Vercel 部署后，立即在手机和电脑都验证一遍，不要假设部署成功=功能正常

### 11.4 Bug 修复文档要求

每次修复 Bug 后，必须在以下文档中记录：

- **CHANGELOG.md**：记录问题现象、根本原因、修复方案
- **PROJECT_PLAN.md**：在"已修复问题记录"表格中添加条目，包含经验教训
- **PROJECT_RULES.md**：如果修复揭示了通用规则/检查清单，补充到本文档

**目的**：同样的坑不踩第二次，形成可复用的问题解决经验。

### 11.5 启动卡住/白屏排查公式（近几次 Bug 修复总结）

**适用场景**：App 启动时白屏、永远停留在 loading/启动界面、React 不挂载。

#### 第一步：最小 React Mount Test

完全绕过所有业务模块，只渲染最基本的 React 组件：
```typescript
ReactDOM.createRoot(root).render(<div>React Mount Test</div>)
```

- 如果最小测试成功 → React 本身没问题，问题在业务模块
- 如果最小测试也失败 → 问题在更基础层面（JS 加载、MIME type、Netlify 配置等）

#### 第二步：ErrorBoundary 一次性捕获渲染异常

如果最小测试成功，添加 React ErrorBoundary 包裹整个 App：
- ErrorBoundary 可以捕获组件渲染时的异常
- 页面上直接显示错误信息和组件调用栈
- 不需要控制台日志，手机端也能看到

**注意**：ErrorBoundary **无法捕获模块加载时的同步异常**。

#### 第三步：检查模块加载时的同步异常

如果 ErrorBoundary 也没捕获到异常，检查是否有模块在**顶层**调用可能抛异常的函数：

```typescript
// ❌ 危险：模块顶层调用可能抛异常的函数
export const supabase = createClient('', '')  // 抛 'supabaseUrl is required'

// ✅ 安全：条件判断，未配置时返回 null
export const supabase = isConfigured ? createClient(url, key) : null
```

**模块加载异常的特点**：
- 在 `import` 阶段就抛出，React 还没开始渲染
- ErrorBoundary 无法捕获
- 会导致所有导入该模块的文件都失败
- 最终表现为：整个应用崩溃，永远显示 index.html 内联的启动界面

**常见元凶**：
- Supabase `createClient('')` 空 URL
- 第三方 SDK 初始化时缺少必要参数
- 模块顶层访问 `window`/`document`（SSR 环境）
- 模块顶层执行网络请求

#### 第四步：验证修复

修复后必须验证：
1. 本地 `tsc --noEmit` 通过
2. 本地 `npm run build` 成功
3. 本地 `npm run preview` 或 `npm run dev -- --host` 正常
4. 手机局域网访问正常
5. 未配置外部服务时也能正常启动（安全降级）

### 11.6 二分定位法（复杂问题排查）

遇到复杂问题时，不要盲目修改，采用二分定位：

1. **最小化测试**：先验证最基础的功能是否正常（如 React 能不能挂载）
2. **逐层恢复**：基础功能正常后，逐层添加业务模块，每加一层测试一次
3. **一次性捕获**：或者用 ErrorBoundary 等工具一次性捕获异常，直接定位问题组件
4. **定位根因后再修复**：不要在没有定位根因的情况下盲目修改
5. **修复后验证**：修复后必须在真实环境（手机/生产）验证，不只是类型检查通过

### 11.7 本地验证优先（开发流程铁律）

**禁止每次小修改都 push 触发生产部署。**

标准流程：
```
修改代码 → tsc检查 → build → preview/局域网测试 → 确认 → commit → 等用户确认 → push部署
```

**局域网测试方法**：
```bash
npm run dev -- --host
# 手机和电脑连同一 Wi-Fi，访问 http://电脑IP:5173
```

**生产部署需用户明确确认**：只有用户说"可以发布了"之后，才执行 `git push` 触发生产部署。

**原因**：
- 频繁生产部署会导致 Service Worker 缓存混乱
- 用户端版本不一致，调试困难
- PWA 调试期间，每次部署都可能让已安装的 PWA 进入不确定状态
- 局域网测试足够验证大多数功能，不消耗部署额度

---

*本文档随项目开发持续更新。规则变更必须记录在 CHANGELOG.md 中。*
