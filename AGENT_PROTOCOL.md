# Personal Life OS — Agent 协作协议

> **版本**：v1.1
> **创建**：2026-09-01
> **定位**：人类 Product Owner、ChatGPT 架构审查层、豆包 Implementation Agent 三者之间的长期协作方式。
> **与其他文档的关系**：
> - PROJECT_PLAN.md = 项目现在是什么
> - PROJECT_RULES.md = 修改项目时应该怎么做
> - CHANGELOG.md = 过去发生了什么
> - **AGENT_PROTOCOL.md（本文档）= 三方如何协作、决策、升级、审查**
> - CHATGPT_HANDOFF.md = 下一次 ChatGPT 接手时的当前快照

---

## 一、三个角色及职责边界

### 1.1 人类 Product Owner（用户）

**职责**：
- 产品方向、优先级、范围的最终决策者
- 真机体验验收（iPhone 16 Pro / iOS 26.3）
- 重大架构决策的最终批准
- 数据模型 / Dexie schema / migration 变更的最终批准
- 生产部署（push / Netlify deploy）的最终批准

**不负责**：
- 具体代码实现
- 日常技术细节决策
- 文档维护

### 1.2 ChatGPT 架构审查层

**职责**：
- 产品需求 → 技术方案的架构设计
- 开发流程本身的审查（不只是代码审查）
- 复杂技术问题的根因分析
- 数据模型、架构、长期风险的评估
- 给豆包 Implementation Agent 的明确、有限、可验证的执行指令
- 审查豆包的实现结果，判断是否符合预期
- 发现流程问题时主动提出并更新本协议

**不负责**：
- 直接编写业务代码
- 日常小功能的实现
- 代替用户做产品决策

### 1.3 豆包 Implementation Agent

**职责**：
- 按照 ChatGPT 给出的明确指令实现代码
- 本地验证（tsc + build + 浏览器测试）
- 文档同步更新（PROJECT_PLAN / CHANGELOG）
- Git commit（不 push）
- 发现指令与实际代码冲突时，以实际代码为准并报告
- 发现超出指令范围的问题时，记录但不擅自扩大范围

**不负责**：
- 产品方向决策
- 架构方案设计
- 重大技术选型
- push / merge / deploy
- 真机验收

---

## 二、开发闭环

```
User（产品需求 / 问题反馈）
    ↓
ChatGPT（架构审查 → 方案设计 → 明确指令）
    ↓
Doubao（实现 → 本地验证 → 文档更新 → commit）
    ↓
Evidence（tsc / build / 浏览器 / 真机 / Production）
    ↓
ChatGPT Review（审查实现结果 → 判断是否符合预期 → 发现流程问题）
    ↓
User Validation（真机验收 / 产品确认 / 部署批准）
```

**闭环原则**：
- 每一轮开发必须有明确的 Evidence，不能只凭"应该可以"
- ChatGPT Review 不仅审查代码，还要审查开发流程本身
- User Validation 是最终门槛，特别是真机体验和产品方向

---

## 三、核心原则

### 3.1 Product Audit First, Phase Second

- 进入任何新阶段前，先做 Product Audit：当前真实产品状态是什么？下一项依赖什么？是否存在阻塞决策？
- 不要因为 PROJECT_PLAN 中写了"Phase X"就机械执行
- 如果审计发现当前方向不明确、有依赖未满足、或有更重要的问题，先解决这些问题
- Roadmap 不是圣旨，实际代码和产品需求优先

### 3.2 Scope Lock

- 每个开发任务必须有明确的范围边界
- Agent 不得因为"技术上能做"自行扩大产品 Scope
- 发现范围外的问题时：记录 → 报告 → 等待决策，不擅自实现
- "顺便优化"、"顺手重构"是 Scope 扩大的常见信号，必须警惕

### 3.3 Change Surface

- 每次修改必须明确 Change Surface：修改了哪些文件？影响哪些模块？哪些调用方需要回归？
- 共享组件修改必须列出所有主要调用方并逐一回归
- 修改范围必须最小化，不为了一个问题重构整个系统
- 修改前先分析依赖关系，修改后验证受影响部分

### 3.4 Evidence Levels

| 级别 | 证据类型 | 可信度 | 适用场景 |
|---|---|---|---|
| L0 | 代码推断 | 低 | 初步判断，不能作为完成依据 |
| L1 | tsc / build 通过 | 中低 | 基础验证，不能证明功能正确 |
| L2 | 自动测试 | 中 | 有测试覆盖的逻辑 |
| L3 | 浏览器实际交互 | 中高 | 桌面端功能验证 |
| L4 | 真机验收（iPhone） | 高 | 移动端体验、iOS 特有问题 |
| L5 | Production 验证 | 最高 | 最终发布确认 |

**规则**：
- 不能用低级别证据代替高级别证据
- "tsc + build 通过"不能证明移动端布局正确、功能可用、体验良好
- 涉及移动端布局的问题必须至少到 L3，iOS 特有问题必须到 L4
- 每次汇报必须明确说明证据级别，不能模糊

### 3.5 发现问题 ≠ 必须立即修复

- 发现问题后先分类：
  - **A. 当前必须修**：核心功能、数据正确性、移动端基本可用性、已完成 UI 的明显回归
  - **B. 后置**：高级视觉、非核心 polish、高级统计、非必要 AI 能力
  - **C. 需要产品决策**：会改变交互逻辑、数据定义、产品方向或长期架构的问题
- A → 自行修复
- B → 记录并继续当前主线
- C → 暂停并向用户汇报
- 不要因为发现一个问题就中断当前任务，除非它属于 A 或 C

### 3.6 需要升级给用户的事项

以下事项必须暂停并向用户汇报，不得自行决定：
1. 产品方向变化、交互逻辑变化、数据定义变化
2. 数据模型 / Dexie schema / migration 变更
3. 重大架构决策、长期技术取舍
4. 需求存在实质性歧义，无法从现有文档推导
5. 必须依赖用户本人的真机 / 视觉 / 主观体验验收
6. 发现可能影响已完成 Layer 1 的重大回归
7. 生产部署（push / merge / deploy）

### 3.7 Deterministic First, AI Second

- 凡是可以通过明确规则、统计、聚合、阈值、时间序列等方式可靠计算的内容，优先由程序完成
- AI 只在需要结合复杂文本、跨领域上下文或无法用确定性规则合理解决的问题上介入
- AI 不允许自动调用，只有用户主动点击"AI 分析"时才允许
- 不要为了"智能"而增加 AI 调用和 token 消耗
- 任何"权重"、"规则"都必须有明确产品理由，不能凭经验随便指定

### 3.8 共享组件修改必须考虑真实调用方回归

- 修改共享组件前，先列出所有主要调用方
- 修改后，逐一验证每个调用方的功能和视觉
- 不能只验证触发修改的那个场景
- "修一炸三"是共享组件修改的常见失败模式，必须主动避免
- 常见共享组件：BottomSheet、GlassInput、GlassButton、GlassCard、SegmentedControl、TodoItem、ScheduleItem 等

### 3.9 数据模型 / Dexie / migration / Supabase sync 的长期风险

- Dexie 表是 schema-less 的，新增字段不需要 migration，但这不是无限加字段的理由
- 新增数据模型前必须审计：是否真的需要持久化？是否可以派生？是否会造成多层重复存储？
- 数据一致性：MoodRecord 是事件事实，Daily Mood 是聚合结果，不能让多个层级各自保存一份互相独立、可能过期的数据
- 优先保持单一可信数据源
- Supabase 同步结构变更会影响云端数据，必须谨慎
- 涉及已有用户数据的 migration 必须先做风险审计

---

## 四、流程审查（ChatGPT 专属职责）

### 4.1 ChatGPT 必须主动审查"开发流程本身"

ChatGPT 不仅审查代码，还要审查：
- 开发流程是否高效？是否存在重复返工？
- 提示词是否膨胀？是否需要简化？
- 同类 Bug 是否反复出现？是否需要系统性修复？
- Agent 是否自行扩大 Scope？是否需要更明确的边界？
- 文档是否漂移？是否与实际代码状态一致？
- 验证流程是否充分？是否存在"tsc 通过就宣布完成"的情况？

### 4.2 流程问题的信号

出现以下现象时，ChatGPT 必须主动提出流程问题：
- 同一个问题被修复多次仍未解决
- Agent 反复实现用户没有要求的功能
- 文档与实际代码状态严重不一致
- 验证只到 tsc + build，没有实际交互验证
- 共享组件修改后没有回归所有调用方
- 提示词越来越长，但执行效果没有提升
- 同类 iOS 问题反复出现，但没有系统性的技术边界记录

### 4.3 执行提示词原则

- ChatGPT 的复杂架构推理不应全部转化为冗长的 Agent Prompt
- 给豆包的执行提示词应保持：明确、有限、可验证
- 明确：做什么、不做什么、完成标准是什么
- 有限：一次只做一个逻辑阶段，不要把 10 个任务塞进一个提示词
- 可验证：完成后如何验证？需要哪些 Evidence？
- 不要让 Agent 自己判断"做什么"，那是 ChatGPT 的职责

---

## 五、长期原则：功能完成后的流程复盘

**每完成一个功能后，不仅检查代码是否正确，还要检查这次开发是否暴露出新的问题：**

1. **产品问题**：这个功能的交互是否合理？是否与整体产品方向一致？
2. **架构问题**：这个功能是否引入了新的技术债？是否影响可扩展性？
3. **数据模型问题**：这个功能是否需要新的数据模型？是否会造成数据一致性问题？
4. **Agent 工作流问题**：这次开发过程中是否有重复返工？是否有 Scope 扩大？验证是否充分？

**如果发现的是"流程问题"，应更新 AGENT_PROTOCOL.md，而不是只修当前代码。**

---

## 六、文档维护规则

| 文档 | 何时更新 | 谁更新 |
|---|---|---|
| PROJECT_PLAN.md | 每次重要任务后，反映当前真实状态 | 豆包 |
| PROJECT_RULES.md | 开发规则变化时 | ChatGPT 提议，用户确认 |
| CHANGELOG.md | 有实际重要修改时追加 | 豆包 |
| **AGENT_PROTOCOL.md** | 协作流程、角色边界、审查机制变化时 | ChatGPT 提议，用户确认 |
| CHATGPT_HANDOFF.md | 重大架构决策、产品方向变化、Agent 工作流变化时 | ChatGPT |

**总原则**：实际代码状态优先于文档。文档必须与代码和实际验证结果保持一致。禁止把"计划开发"、"组件已存在"、"理论支持"写成"功能已完成"。

---

## 七、版本历史

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.1 | 2026-09-01 | 按 Product Owner 确认统一 Evidence Levels 为 L0-L5：L0 代码推断、L1 tsc/build、L2 自动测试、L3 浏览器、L4 真机、L5 Production |
| v1.0 | 2026-09-01 | 初始版本。建立三方协作协议、开发闭环、核心原则、流程审查机制、长期复盘原则 |
